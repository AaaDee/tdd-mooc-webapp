{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "NODE_ENV=test jest --verbose --runInBand",
    "build": "rm -rf build && cd ../frontend && npm run build --prod && cp -r build ../backend"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jest": "^29.4.3",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "supertest": "^6.3.3"
  }
}
